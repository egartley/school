1) Before the modification, the running time was O(1). After the modification, the run time is still O(1).

2) The running time is O(1)

3) The running time is O(1) amortized

4) The running time is O(n)

5) Functions in Part II both randomly re-order the elements of the dynamic array. Possible names include "scramble()", "mix()", and "reorder()".

6) The running time of lab1_v1 is O(n^2) and lab1_v2 is O(n).